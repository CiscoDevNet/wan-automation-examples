meta {
  name: Retrieve interfaces from Device
  type: http
  seq: 11
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/interface?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~vpn-id: {{vpnId}}
  ~ifname: {{ifName}}
  ~af-type: {{afType}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
