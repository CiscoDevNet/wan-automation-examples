meta {
  name: Retrieve interfaces serial -
  type: http
  seq: 4
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/interface/serial?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~vpn-id: {{vpnId}}
  ~ifname: {{ifName}}
  ~af-type: {{afType}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
