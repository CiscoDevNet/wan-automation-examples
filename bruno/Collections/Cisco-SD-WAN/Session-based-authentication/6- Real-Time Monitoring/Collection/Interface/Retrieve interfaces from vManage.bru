meta {
  name: Retrieve interfaces from vManage
  type: http
  seq: 1
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/interface/synced?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~vpn-id: {{vpnId}}
  ~ifname: {{ifName}}
  ~af-type: {{afType}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
