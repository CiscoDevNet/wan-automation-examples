meta {
  name: Retrieve interface packet size statistics
  type: http
  seq: 6
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/interface/pkt_size?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~vpn-id: {{vpnId}}
  ~ifname: {{ifName}}
  ~af-type: {{afType}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
