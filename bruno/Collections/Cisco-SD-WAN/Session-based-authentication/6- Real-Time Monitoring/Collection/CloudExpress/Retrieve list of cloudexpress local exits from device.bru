meta {
  name: Retrieve list of cloudexpress local exits from device
  type: http
  seq: 2
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/cloudx/localexits?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~vpn-id: 
  ~application: 
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
