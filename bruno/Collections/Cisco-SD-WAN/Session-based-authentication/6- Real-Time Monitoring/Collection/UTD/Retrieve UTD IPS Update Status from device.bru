meta {
  name: Retrieve UTD IPS Update Status from device
  type: http
  seq: 5
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/utd/ips-update-status?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
