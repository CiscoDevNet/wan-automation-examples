meta {
  name: Retrieve UTD Engine Status from device
  type: http
  seq: 12
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/utd/engine-status?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
