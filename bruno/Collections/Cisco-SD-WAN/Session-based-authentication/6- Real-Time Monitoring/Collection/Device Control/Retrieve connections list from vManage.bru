meta {
  name: Retrieve connections list from vManage
  type: http
  seq: 6
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/control/synced/connections?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~peer-type: 
  ~system-ip: 
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
