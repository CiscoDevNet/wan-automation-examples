meta {
  name: Retrieve connection statistics from device
  type: http
  seq: 19
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/control/statistics?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
