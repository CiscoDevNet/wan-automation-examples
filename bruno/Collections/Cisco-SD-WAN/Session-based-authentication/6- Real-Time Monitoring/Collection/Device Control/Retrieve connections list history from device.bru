meta {
  name: Retrieve connections list history from device
  type: http
  seq: 7
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/control/connectionshistory?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~peer-type: 
  ~system-ip: 
  ~local-color: {{localColor}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
