meta {
  name: Retrieve local properties list
  type: http
  seq: 13
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/control/localproperties?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
