meta {
  name: Retrieve port hop colors
  type: http
  seq: 16
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/control/waninterface/color?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
