meta {
  name: Retrieve WAN interface list from vManage
  type: http
  seq: 12
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/control/synced/waninterface?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
