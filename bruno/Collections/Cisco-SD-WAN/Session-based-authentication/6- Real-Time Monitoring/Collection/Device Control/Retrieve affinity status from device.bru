meta {
  name: Retrieve affinity status from device
  type: http
  seq: 18
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/control/affinity/status?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
