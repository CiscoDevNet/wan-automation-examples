meta {
  name: Retrieve connections list from device
  type: http
  seq: 20
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/control/connections?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~peer-type: 
  ~system-ip: 
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
