meta {
  name: Retrieve affinity config from device
  type: http
  seq: 17
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/control/affinity/config?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
