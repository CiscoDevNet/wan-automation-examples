meta {
  name: Retrieve valid vSmart list from device
  type: http
  seq: 8
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/control/validvsmarts?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
