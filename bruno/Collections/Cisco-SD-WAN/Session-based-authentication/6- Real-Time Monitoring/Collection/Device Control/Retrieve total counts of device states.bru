meta {
  name: Retrieve total counts of device states
  type: http
  seq: 15
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/control/count?isCached={{isCached}}
  body: none
  auth: none
}

params:query {
  isCached: {{isCached}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
