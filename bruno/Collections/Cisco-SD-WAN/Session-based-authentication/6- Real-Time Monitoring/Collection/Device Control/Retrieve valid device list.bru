meta {
  name: Retrieve valid device list
  type: http
  seq: 10
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/control/validdevices?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
