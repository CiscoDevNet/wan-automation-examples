meta {
  name: Retrieve IPsec Pairwise Key Local SA entry from device
  type: http
  seq: 10
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/ipsec/pwk/localsa?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~remote-tloc-address: 
  ~remote-tloc-color: 
  ~local-tloc-color: 
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
