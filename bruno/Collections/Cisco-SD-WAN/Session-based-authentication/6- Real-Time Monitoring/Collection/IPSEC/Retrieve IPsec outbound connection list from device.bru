meta {
  name: Retrieve IPsec outbound connection list from device
  type: http
  seq: 2
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/ipsec/outbound?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~remote-tloc-address: 
  ~remote-tloc-color: 
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
