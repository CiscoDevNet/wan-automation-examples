meta {
  name: Retrieve IPsec Pairwise Key Inbound entry from device
  type: http
  seq: 11
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/ipsec/pwk/inbound?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~remote-tloc-address: 
  ~remote-tloc-color: 
  ~local-tloc-color: 
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
