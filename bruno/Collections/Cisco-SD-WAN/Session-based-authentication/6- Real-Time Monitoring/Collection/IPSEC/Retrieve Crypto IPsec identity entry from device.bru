meta {
  name: Retrieve Crypto IPsec identity entry from device
  type: http
  seq: 7
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/ipsec/identity?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~remote-tloc-address: 
  ~remote-tloc-color: 
  ~local-tloc-color: 
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
