meta {
  name: Retrieve Crypto IPsec IKEv2 SA entry from device
  type: http
  seq: 9
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/ipsec/ikev2?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
