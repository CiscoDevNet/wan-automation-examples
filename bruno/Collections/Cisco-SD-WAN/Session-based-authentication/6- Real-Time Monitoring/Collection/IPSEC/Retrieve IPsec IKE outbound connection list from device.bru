meta {
  name: Retrieve IPsec IKE outbound connection list from device
  type: http
  seq: 5
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/ipsec/ike/outbound?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
