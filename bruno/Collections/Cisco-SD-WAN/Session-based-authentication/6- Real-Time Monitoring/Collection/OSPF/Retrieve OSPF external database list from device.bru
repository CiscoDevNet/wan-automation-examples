meta {
  name: Retrieve OSPF external database list from device
  type: http
  seq: 7
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/ospf/databaseexternal?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
