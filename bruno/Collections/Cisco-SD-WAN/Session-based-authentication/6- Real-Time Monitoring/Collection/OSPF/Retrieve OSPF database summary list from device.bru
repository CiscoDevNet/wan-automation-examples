meta {
  name: Retrieve OSPF database summary list from device
  type: http
  seq: 6
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/ospf/databasesummary?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
