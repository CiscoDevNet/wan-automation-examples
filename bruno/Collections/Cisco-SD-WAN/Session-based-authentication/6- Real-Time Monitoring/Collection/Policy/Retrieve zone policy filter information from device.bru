meta {
  name: Retrieve zone policy filter information from device
  type: http
  seq: 16
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/policy/zonepolicyfilter?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
