meta {
  name: Retrieve zone based firewall statistics from device
  type: http
  seq: 14
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/policy/zbfwstatistics?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
