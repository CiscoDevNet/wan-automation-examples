meta {
  name: Retrieve zone based firewall statistics from device_1
  type: http
  seq: 17
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/policy/zbfwdropstatistics?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
