meta {
  name: Retrieve rewrite associations information from device
  type: http
  seq: 13
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/policy/rewriteassociations?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
