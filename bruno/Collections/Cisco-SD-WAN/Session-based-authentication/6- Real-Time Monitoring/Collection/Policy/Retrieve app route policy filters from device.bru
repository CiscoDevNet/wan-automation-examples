meta {
  name: Retrieve app route policy filters from device
  type: http
  seq: 10
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/policy/approutepolicyfilter?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
