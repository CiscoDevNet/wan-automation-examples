meta {
  name: Retrieve zone policy zone pair sessions from device
  type: http
  seq: 15
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/policy/zonepairsessions?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
