meta {
  name: Retrieve data policy filters from device
  type: http
  seq: 9
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/policy/datapolicyfilter?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
