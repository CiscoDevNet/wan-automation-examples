meta {
  name: Retrieve App Hosting Utilization from device
  type: http
  seq: 3
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/app-hosting/utilization?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
