meta {
  name: Retrieve IPv6 Neighbors from device
  type: http
  seq: 1
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/ndv6?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~vpn-id: {{vpnId}}
  ~if-name: 
  ~mac: 
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
