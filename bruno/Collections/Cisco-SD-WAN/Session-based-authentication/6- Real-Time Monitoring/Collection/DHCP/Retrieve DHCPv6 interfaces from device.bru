meta {
  name: Retrieve DHCPv6 interfaces from device
  type: http
  seq: 4
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/dhcpv6/interface?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
