meta {
  name: Retrieve connection list from device
  type: http
  seq: 8
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/orchestrator/connections?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
