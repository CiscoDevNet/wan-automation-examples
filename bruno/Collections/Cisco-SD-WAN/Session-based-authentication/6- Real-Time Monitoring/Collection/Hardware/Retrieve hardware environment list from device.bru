meta {
  name: Retrieve hardware environment list from device
  type: http
  seq: 8
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/hardware/environment?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
