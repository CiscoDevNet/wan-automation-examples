meta {
  name: Retrieve hardware environment list from vManage
  type: http
  seq: 9
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/hardware/synced/environment?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
