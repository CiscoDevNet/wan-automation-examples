meta {
  name: Retrieve ietf routing list from device
  type: http
  seq: 12
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/ip/ipRoutes?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~name: 
  ~address-family: 
  ~destination-prefix: 
  ~outgoing-interface: 
  ~source-protocol: 
  ~next-hop-address: 
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
