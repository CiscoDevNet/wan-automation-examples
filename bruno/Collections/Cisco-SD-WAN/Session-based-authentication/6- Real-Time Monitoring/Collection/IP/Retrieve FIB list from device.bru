meta {
  name: Retrieve FIB list from device
  type: http
  seq: 1
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/ip/fib?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~vpn-id: {{vpnId}}
  ~address-family: 
  ~prefix: 
  ~tloc: 
  ~color: {{color}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
