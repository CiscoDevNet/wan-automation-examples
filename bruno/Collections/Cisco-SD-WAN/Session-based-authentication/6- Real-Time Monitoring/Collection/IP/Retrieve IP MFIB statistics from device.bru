meta {
  name: Retrieve IP MFIB statistics from device
  type: http
  seq: 5
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/ip/mfibstats?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
