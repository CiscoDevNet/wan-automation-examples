meta {
  name: Retrieve NAT64 interface list from device
  type: http
  seq: 11
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/ip/nat64/translation?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
