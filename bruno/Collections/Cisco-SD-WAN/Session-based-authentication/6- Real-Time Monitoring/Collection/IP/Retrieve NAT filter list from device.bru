meta {
  name: Retrieve NAT filter list from device
  type: http
  seq: 9
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/ip/nat/filter?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~nat-vpn-id: 
  ~nat-ifname: 
  ~private-source-address: 
  ~proto: 
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
