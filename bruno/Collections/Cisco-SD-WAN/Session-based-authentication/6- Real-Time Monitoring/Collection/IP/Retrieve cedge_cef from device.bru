meta {
  name: Retrieve cedge_cef from device
  type: http
  seq: 2
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/ip/cedge_cef?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~ip-addr: 
  ~af: 
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
