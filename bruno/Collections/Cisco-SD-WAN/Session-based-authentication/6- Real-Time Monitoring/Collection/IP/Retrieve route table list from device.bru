meta {
  name: Retrieve route table list from device
  type: http
  seq: 3
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/ip/routetable?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~vpn-id: {{vpnId}}
  ~address-family: 
  ~prefix: 
  ~protocol: 
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
