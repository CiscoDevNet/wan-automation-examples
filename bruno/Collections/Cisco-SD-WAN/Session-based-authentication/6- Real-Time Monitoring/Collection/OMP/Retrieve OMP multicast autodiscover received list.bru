meta {
  name: Retrieve OMP multicast autodiscover received list
  type: http
  seq: 11
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/omp/mcastautodiscoverrecv?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
