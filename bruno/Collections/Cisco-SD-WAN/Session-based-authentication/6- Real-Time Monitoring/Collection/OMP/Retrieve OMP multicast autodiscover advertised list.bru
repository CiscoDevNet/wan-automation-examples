meta {
  name: Retrieve OMP multicast autodiscover advertised list
  type: http
  seq: 12
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/omp/mcastautodiscoveradvt?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
