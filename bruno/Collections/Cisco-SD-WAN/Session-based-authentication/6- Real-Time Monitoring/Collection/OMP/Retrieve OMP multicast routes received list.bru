meta {
  name: Retrieve OMP multicast routes received list
  type: http
  seq: 13
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/omp/mcastroutesrecv?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
