meta {
  name: Retrieve CloudExpress routes received list
  type: http
  seq: 15
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/omp/cloudx?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
