meta {
  name: Retrieve OMP summary
  type: http
  seq: 9
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/omp/summary?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
