meta {
  name: Retrieve OMP multicast routes advertised list
  type: http
  seq: 14
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/omp/mcastroutesadvt?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
