meta {
  name: Retrieve device system status list from vManage
  type: http
  seq: 8
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/system/synced/status?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
