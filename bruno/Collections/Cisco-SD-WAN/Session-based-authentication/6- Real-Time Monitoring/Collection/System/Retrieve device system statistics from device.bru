meta {
  name: Retrieve device system statistics from device
  type: http
  seq: 7
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/system/statistics?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
