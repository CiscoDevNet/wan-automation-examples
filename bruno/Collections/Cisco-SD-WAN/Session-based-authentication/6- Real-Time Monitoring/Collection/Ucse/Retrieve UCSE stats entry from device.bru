meta {
  name: Retrieve UCSE stats entry from device
  type: http
  seq: 1
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/ucse/stats?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~remote-tloc-address: 
  ~remote-tloc-color: 
  ~local-tloc-color: 
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
