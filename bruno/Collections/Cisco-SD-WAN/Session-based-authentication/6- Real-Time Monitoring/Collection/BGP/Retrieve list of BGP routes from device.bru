meta {
  name: Retrieve list of BGP routes from device
  type: http
  seq: 2
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/bgp/routes?deviceId={{deviceId}}&vpn-id={{vpnId}}&prefix=&nexthop=
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  vpn-id: {{vpnId}}
  prefix: 
  nexthop: 
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
