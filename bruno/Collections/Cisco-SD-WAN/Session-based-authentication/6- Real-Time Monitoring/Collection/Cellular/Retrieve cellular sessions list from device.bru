meta {
  name: Retrieve cellular sessions list from device
  type: http
  seq: 8
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/cellular/sessions?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~if-name: 
  ~ipv4-dns-pri: 
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
