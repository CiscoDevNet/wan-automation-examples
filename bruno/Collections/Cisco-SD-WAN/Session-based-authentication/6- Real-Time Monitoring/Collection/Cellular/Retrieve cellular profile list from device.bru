meta {
  name: Retrieve cellular profile list from device
  type: http
  seq: 7
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/cellular/profiles?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
