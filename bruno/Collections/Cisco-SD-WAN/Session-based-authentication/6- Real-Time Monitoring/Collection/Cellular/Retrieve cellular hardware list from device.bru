meta {
  name: Retrieve cellular hardware list from device
  type: http
  seq: 5
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/cellular/hardware?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
