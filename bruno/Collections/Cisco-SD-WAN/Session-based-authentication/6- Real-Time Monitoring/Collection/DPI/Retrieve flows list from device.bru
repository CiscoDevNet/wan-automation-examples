meta {
  name: Retrieve flows list from device
  type: http
  seq: 5
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/dpi/flows?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~vpn-id: {{vpnId}}
  ~src-ip: 
  ~application: 
  ~family: 
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
