meta {
  name: Retrieve list of supported applications from device
  type: http
  seq: 2
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/dpi/supported-applications?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~application: 
  ~family: 
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
