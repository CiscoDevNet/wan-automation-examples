meta {
  name: Retrieve DPI applications from device
  type: http
  seq: 1
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/dpi/applications?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~vpn-id: {{vpnId}}
  ~application: 
  ~family: 
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
