meta {
  name: Retrieve list of BFD sessions from device
  type: http
  seq: 2
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/device/bfd/sessions?deviceId={{deviceId}}
  body: none
  auth: none
}

params:query {
  deviceId: {{deviceId}}
  ~system-ip: {{systemIp}}
  ~color: {{color}}
  ~local-color: {{localColor}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
