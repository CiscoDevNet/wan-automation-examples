meta {
  name: CloudExpress Service
  type: http
  seq: 5
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/data/device/statistics/cloudxstatistics?startDate={{startDate}}&endDate={{endDate}}&timeZone={{timeZone}}&count={{count}}
  body: none
  auth: none
}

params:query {
  startDate: {{startDate}}
  endDate: {{endDate}}
  timeZone: {{timeZone}}
  count: {{count}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
