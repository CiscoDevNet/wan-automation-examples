meta {
  name: Bridge Interfaces
  type: http
  seq: 3
}

get {
  url: https://{{vmanage}}:{{port}}/dataservice/data/device/statistics/bridgeinterfacestatistics?startDate={{startDate}}&endDate={{endDate}}&timeZone={{timeZone}}&count={{count}}
  body: none
  auth: none
}

params:query {
  startDate: {{startDate}}
  endDate: {{endDate}}
  timeZone: {{timeZone}}
  count: {{count}}
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
}
