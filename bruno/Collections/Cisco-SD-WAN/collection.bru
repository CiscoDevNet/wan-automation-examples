docs {
  # Cisco SD-WAN Manager API
  
  Use this collection to test SD-WAN Manager APIs.
  
  Authenticate to Manager:
  - `1-Authentication/Authentication` to authenticate using user/password
  - `1-Authentication/Token` to get a token (X-XSRF-TOKEN)
  
  Use and test APIs ...
}
